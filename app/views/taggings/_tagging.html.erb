<%= div_for(tagging) do %>
  <%= link_to('-', tagging_path(tagging), :remote => true, :method => :delete, :class => "remove") %>
  <%= link_to(tagging.tag.name, tag_snippets_path(tagging.tag), :class => tag_score_class(tagging.tag.score)) %>
  <%= total_for(tagging.tag.taggings_count) %>
<% end %>